- [Project consolidated to Animator](project-consolidation-animator.md) — voxel-engine/arcade/carrier DELETED; Animator is the sole app at `/`; api-server+libs kept; many memory topic files now describe deleted code (treat as historical).
- [Railway game-server hosting](railway-game-server.md) — game relays hosted on Railway (auto-wss/TLS); RAILWAY_API_TOKEN is a TEAM token (Bearer GraphQL only, not CLI); deploy via a project token + `railway up`.
- [Animator lib design](animator-lib.md) — @workspace/animator single-active-clip rig; engine owns world translation so suppress WASD during rooted clips; movement verbs fall back to unarmed set.
- [Animator controls persistence](animator-controls-persistence.md) — the engine mutates camera state outside React, so settings must persist to storage and be re-read at every mount, never pushed from stale React state.
- [Animator Explorer default](animator-explorer-default.md) — Explorer rig vendored + FBX self-hosted (no @workspace/animator|assets); other @workspace libs imported freely; chars polymorphic via Avatar.
- [Arcade engine dispatch](arcade-engine-dispatch.md) — tag HUD snapshots with game id behind one route; add "three" to Vite resolve.dedupe when adding the first jsm addon.
- [Space-shooter netcode](space-shooter-netcode.md) — online cabinet: shared pure sim run verbatim both ends; interpolation clock must advance per-frame not per-snapshot; guard NaN input; Reserved VM not Autoscale.
- [Flight control screen basis](flight-control-screen-basis.md) — chase-cam turn signs derive from camera screen-right (cross(up,eye-target)), not world +X; fix inversion in client input not the shared sim; chase cam along +Z makes screen-right = -X.
- [Puter guest auth](puter-guest-auth.md) — signIn({attempt_temp_user_creation:true}) makes a guest; user.is_temp flags it; a plain signIn() upgrades in place keeping KV data.
- [Voxel realm split](voxel-realm-split.md) — voxel-engine packs overworld (cy>=0) + underworld (cy<0) into one world; boundary at Y=0 must stay chunk-aligned.
- [Explorer RPG UI suite](explorer-rpg-ui.md) — additive ARPG overlay on the Explorer cabinet; panel hotkeys avoid engine keys, engine←store via setters, KV persistence best-effort.
- [Animator locomotion blend](animator-locomotion-blend.md) — weight-blended idle/walk/run sits ALONGSIDE the single-clip SM; collapseToDominant/beginSingle handoff prevents weight-ownership conflicts; stride clips phase-synced (don't crossfade).
- [VFX quarks lib](vfx-quarks-lib.md) — shared @workspace/vfx wraps three.quarks; systems self-dispose unless under a Scene; clone() shares materials; dispose VFX before host scene sweep.
- [Asset texture-atlas matching](asset-texture-atlas-matching.md) — Synty packs share atlases; match by material.map.name not mesh name, else every prop wears t-boat.
- [Voxel item content](voxel-item-content.md) — play-mode item ids are `${mat}_${cls}` (wood_pickaxe); fromData drops unknown ids silently; keep dur on save; craft() is atomic snapshot+rollback.
- [Voxel third-person player](voxel-thirdperson-player.md) — Controls owns both camera framings; getView returns EYE not raw camera; held/avatar mutually exclusive by view; eye-origin actions; adding a @workspace lib needs a Vite restart.
- [Voxel creatures + game modes](voxel-creatures-gamemode.md) — rulesFor(mode) is the single source; enforce xp/death on EVERY source not just creature melee; RMB hold=aim tap=place; creatures non-persisted per mount.
- [Voxel terrain generation](voxel-terrain-generation.md) — terrain must be pure-data + seed-deterministic; cross-chunk structures (trees) are stamped via a margin pass, not owned; use hash01 not per-voxel closures.
- [Voxel worker pipeline](voxel-worker-pipeline.md) — gen+mesh run in a THREE-free worker; generator crosses as a spec not instance; copy (don't transfer) chunk voxels; main only uploads geometry.
- [Explorer cabinet](explorer-cabinet.md) — ground ray from player.y+lift (not sky) to avoid overhang snapping; dispose GLTF scenes that resolve after teardown; Digit3 dagger-ring blades own their own geo+mat so fired ones transfer to projectiles safely; unify ring teardown across all weapon swaps.
- [Explorer combat foundation](explorer-combat-foundation.md) — one applyDamage(Durability) mutates health in place; enemies wrap archetype.toughness; managers own meshes; store→engine via setters only.
- [Three.js disposal](three-disposal.md) — dispose at every mid-game removal; NEVER disposeObject3D the raw loadAsset model (shared cache) — only owned clones/animator in cabinet launchers.
- [Faction ring lifecycle](faction-rings.md) — magic-ring GLB templates loaded once; per-entity rings are clones sharing materials; detach + tag clones to skip per-actor dispose; free templates once; selection ring owns its materials.
- [Voxel material id](voxel-stylized-material-id.md) — per-vertex `mat` float (derived from block.meta, constant per block id) rides greedy meshing to drive per-block-type procedural shader detail.
- [Zombie third-person rig](zombie-thirdperson-rig.md) — arcade GLB is skinless (clips drive node transforms); attach/hide weapons by node name; camYaw drives orbit+facing+centre-ray aim; grip/camera need manual GPU tuning.
- [Zombie DEV editor](zombie-dev-editor.md) — in-scene TransformControls editor + persisted DevConfig; seed yaw FROM rig not default-0; rack slots pure-translation; getHelper() in three 0.184.
- [Zombie gameplay feel](zombie-gameplay-feel.md) — smoothed vel must stay consistent across direct-move overrides (roll); reticle + bullet share ONE enemyHitSphere; AnimatedActor base-state is queued+dwelled.
- [Animator Danger Room artifact](animator-danger-room.md) — self-contained three.js artifact; scaffold ships an unused @workspace dep (remove from package.json); SkeletonHelper is core not Addons; Timer not shipped; guard async swaps with a load token.
- [Animator GLB-driven VFX](animator-glb-vfx.md) — Danger Room VFX clone shared GLB templates (slash arcs, lightning); shared clones must NOT dispose template geo/textures, only the cloned material; fireball GLB packs 4 colors along Z.
- [Animator lunge/dash motion](animator-dangerroom-lunge.md) — dash/kick skills: real clip drives body + our VFX, eased spline timed to clip length, impact mid-lunge, no forced snap-slide; weaponless+meleeStyle char flags.
- [Voxel melee combat](voxel-epicfight-combat.md) — EpicFight-style melee = PlayerController intents (combat:*) → MeleeCombatSystem resolver vs creatures; impact via fx:hitstop/fx:shake/combat:impact; creatures telegraph (windup); views only animate; superseded the EF dummy.
- [Voxel streaming integrity](voxel-streaming-integrity.md) — player physics treats not-yet-loaded chunks as solid (moveAABB solidIfUnloaded) so you can't fall into the void or walk off a finite world's edge; mobs/projectiles keep default off.
- [Studio settings schema](studio-settings-schema.md) — new StudioSettings fields rarely need a schema bump; scope each load-time migration to its old schema range, never the bare `< SETTINGS_SCHEMA` (it once wiped invertY on every bump).
- [Animator touch + status FX](animator-touch-status.md) — conditionally-mounted touch controls must reset ALL latched input on unmount; analog joystick blends atop keyboard gated on moveX/moveY!=0.
- [Voxel Studio control/camera feel](voxel-studio-controls.md) — Q=swap Build/Play (reserved, global), drop→KeyB, absorb intentionally on in ALL modes, TPS orbit deadband kills "zoom-around"; camera feel is sandbox-unverifiable.
- [Carrier game architecture](carrier-game.md) — dedicated artifact on carrier-owned `carrier-net`; player flies a fighter + owns a mothership-anchor; fleet AI is server-only, pure & deterministic (tick+seeded rand); one kind-aware stepShip; clients only interpolate.
- [Animator weapon class](animator-weapon-class.md) — new procedural WeaponClass must thread union + exhaustive WEAPON_SETS + WeaponAnimSet + loader preload; CharacterDef.defaultWeapon auto-equips on spawn.
- [EpicFight defensive model](epicfight-defensive-model.md) — resolveDefense pure fn + CombatController.applyAttack; host must call applyVulnerableState on ATTACKER after reading result.attackerReaction; 9 reaction FBX clips in animations/reactions/.
- [Pistol Kiter kit](pistol-kiter-kit.md) — per-character primary kits branch Studio.attack() on a CharacterDef data flag before the generic combo; procedural rig plays bespoke one-shots via verb-named playClipOnce→Animator.playAction; dummies passive so stun is cosmetic.
- [Danger Room sparring AI](animator-sparring-ai.md) — difficulty-scaled opponents share player's combat.ts falloff; Targets→Studio coupling one-way via SparringContext; difficulty is data (DIFFICULTY_PROFILES); passive=old dummy; RMB block/parry; manual-verify only.
- [Skyfall apex/pending](skyfall-apex-pending.md) — apex-gated aerial ability must cancel competing aerial states on launch (apex only fires in gravity branch) AND carry a fail-safe timer on its pending-payload flag or future casts deadlock.
- [Animator target-lock & HUD projection](animator-target-lock.md) — RMB-hold lock stance overrides wantFacing LATE for strafe; motion-math attacks map to signed dash(); NDC z<=1 alone leaks behind-camera HUD frames (also need z>=-1 & |x|,|y|<=1).
- [Animator procedural clip registration](animator-procedural-clip-registration.md) — new FBX verb needs ActionKey + catalog (UNIVERSAL_MOVEMENT→unarmed.actions for resolveMovement fallback) + Animator method + playClipOnce case + VERBS, all in lockstep; GLB chars no-op via hasClip.
- [Animator GLB sub-clip combos](animator-glb-subclip-combo.md) — split one GLB combo into per-click hits via a fraction-based sub-clip registry sliced from a cached parent; combo[] entries resolve through actions (NOT VERBS) so adding a combo/parry clip is data-only.
- [Danger Room physics + faction NPCs](animator-danger-room-physics.md) — renderer-agnostic Rapier core for r3f rewrite; empty-boot enforced by Targets count default 0 (not App); NPCs carry enemy/ally faction; spawning additive.
- [Animator Dungeon Mode](animator-dungeon-mode.md) — door portal swaps Danger Room for a loaded dungeon; player collision GATED behind a provider (null=original feel); down-ray navmesh: hits[0] is the roof, scan for lowest up-facing floor w/ headroom; enterDungeon must be try/finally exception-safe.
- [Offline 3D model verification](offline-3d-model-verification.md) — headless browser has no WebGL + cabinets are Puter-gated; verify GLB/OBJ facing/size by parsing with three in Node and software-rasterizing silhouettes to PNG.
- [Carrier asset-id wiring](carrier-asset-wiring.md) — catalog id = path minus extension (single-file-in-named-folder packs have a doubled segment); a wrong id silently shows the procedural fallback; guard with findAsset + the factionAssets test.
- [Animator Voxel Editor controls](animator-voxel-editor-controls.md) — Danger Room VoxelEditor: LMB hold-drag builds (stack/wall/ramp, never camera, dedupe by cell), RMB drag orbits + RMB click erases, deploy=click-only.
- [Voxel Editor NPC previews](animator-editor-npc-previews.md) — editor NPCs clone ONE shared idle-posed template (SkeletonUtils) + mount real weapon GLB; per-instance dispose only unmounts cloned weapon mats, never the shared template/templateCache.
- [Animator GLB Mixamo retarget](animator-glb-mixamo-retarget.md) — mixing GLB clips into the FBX library via GLB_CLIP_IDS; retarget is rotation-only (drop position tracks: source has different bone proportions) + normalize mixamorig:Bone_NN → mixamorigBone.
- [Danger Room collision + impact VFX](animator-danger-room-collision-vfx.md) — null-collision path needs an obstacle push-out (guarded by !collision) with a 2nd wall-slide pass for corners; impactExplode fires every melee hit so its gl_PointSize MUST be clamped or it whites out near-camera.
- [Animator Scene Editor](animator-scene-editor.md) — general 3D editor (3rd door): EditorScene is sole source of truth, every mutating setter must emit() or UI goes stale; poll for async rig clips; r0.184 gizmo uses getHelper(); dispose grid/axes helpers manually.
- [Animator dock shell + menubar](animator-dock-shell.md) — reusable components/dock (useDockLayout+DockSurface+ToolMenubar); host legacy panels via `chrome={false}` flag returning bare body; dock layout (not booleans) is the single visibility source, show-side-effects in one toggle helper gated on pre-toggle isVisible.
- [Animator Gallery / posting](animator-gallery-posting.md) — voxel maps ALWAYS post kind "dungeon" (loadable/playable); Scene Editor scenes post "scene" but are view-only (no EditorScene.importJSON yet); load via pendingMapRef.
- [Animator first-person view + aim](animator-first-person-view.md) — FP hides avatar.root via shared ref, so any Controller teardown in FP must restore visibility first or it leaks hidden; aim math lives in src/three/aim/AimSystem.ts; toggle=KeyB.
- [Animator blade-trail vs flame-trail](animator-blade-trail.md) — basic melee swings use a thin additive ribbon (clean slash); the 1400-particle GPU flame trail is reserved for flame skills (it bloomed out swings; no postFX bloom pass exists); swingColor set in BOTH swing paths.
- [Animator target indicators + AOE telegraph](animator-target-indicators.md) — recolor shared ring GLB via alphaMap+additive (not multiplicative); skill swings (not just bosses) telegraph + resolve VFX at impact; capture pendingSkill before reset.
- [Animator time-scale & feel knobs](animator-time-scale-feel.md) — global slow-mo scales dt but FPS+updateNet use raw; combo lock/window from clip dur not constants; Studio remount MUST re-apply setParams+setTimeScale or UI desyncs.
- [Animator offense-fail recovery](animator-offense-fail-recovery.md) — when YOUR swing is blocked/parried/dodged: recoverLock taxes a tempo beat + recoils along player→hitPos; gate ALL offense incl useSkill; shield-break = no tax.
- [Animator editor import + wardrobe](animator-editor-import-wardrobe.md) — one importFile() routes glb/gltf/fbx/obj/zip/bbmodel (vendored loaders); skin tex flipY=false + never dispose shared maps; zip needs size caps; gltf-in-zip via LoadingManager blob URLs; gear attach keeps parentId synced.
- [Animator dodge-roll + double-tap input](animator-dodge-roll-input.md) — double-tap edge-detects fresh presses (ignore OS repeat); strafe roll must re-faceToward(forward) AFTER dash or clip dives; i-frame chaining needs a cooldown; rollDir? optional on Avatar (GLB no-ops); KeyT→M3, KeyZ→straight stab.
- [Animator A.L.E. social/video](animator-ale-feed.md) — A.L.E. = GRUDOX entertainment AI; in-app draft feed + match recap + 10s narrated review (browser TTS), all preview-only; ad videos gated on AI skill-meta (parry/block/dodge/timing); real posting needs integrations.
- [Animator instanced billboard particles](animator-instanced-billboard-particles.md) — SmokeFx: one-draw-call GPU billboards owned by Vfx; billboard mode in iQuat.w (3 face/4 cyl/6 streak); premultiplied additive = no CPU sort; presets are a 3-file lockstep; smokePop wired into impact().
- [Animator instant replay](animator-instant-replay.md) — A.L.E. duel replays record real per-frame poses (root+bone-local) to a ring, re-pose in slow-mo decoupled (Studio.loop early-returns), MUST restore live pose on end (dead corpse root drifts otherwise); buffer+sampling pure & unit-tested.
- [Animator vitest OOM](animator-vitest-oom.md) — full animator suite gets OOM-killed in agent bash at default parallelism; run with NODE_OPTIONS max-old-space + --pool=forks --no-file-parallelism.
- [Animator combo forward MM](animator-combo-forward-mm.md) — "MM" = motion-math (100MM=1m, MM_TO_M); combo forward gap-closer rides momentum-lunge swings (stage>0) in doComboHit, opener stays target-clamped.
- [Animator Dressing Room weapon arsenal](animator-weapon-arsenal-panel.md) — Dressing Room rig has NO visible weapon by default (setWeaponId=clips only); equip must mirror Studio.applyWeapon; WEAPON_GRIPS shares def.grip ref but grip-less weapons need re-assign; tag mounts __libWeapon to dodge re-register.
- [Animator deployable GLB props](animator-deployable-props.md) — props are a "prop" DeployableKind; uploaded GLBs MUST be normalized at load (fit targetHeight + recentre + base→Y=0), shared template cloned (never dispose clones); new kind = lockstep edits incl BOTH BrushState literals.
- [Animator PvP server authority](animator-pvp-authority.md) — pvp duels = server-owned HP + validated hits (range/rate/attack-window) + anti-teleport clampMove, NOT full resim; victim-reported guard with avoid cooldown; client reads HP from snapshots, self-hit = VFX only, death/respawn server-driven; coop unchanged.
- [Animator canonical fighter height](animator-canonical-height.md) — CHARACTER_HEIGHT_M in types.ts is the ONE rig-normalize height; all "fit to 1.8m" literals must route through it; Targets spacing/spell-range derive from it.
- [Animator predictive AI aim](animator-predictive-aim.md) — per-dummy smoothed target velocity leads casts (capped to a fraction of distance so jukes still dodge) + dash gap-close; reset estimate on target swap or lead spikes.
- [Animator OWR respect-range](animator-owr-respect-range.md) — per-weapon Optimal-Weapon-Range verdicts scale dmg both sides; apply damageMul unconditionally (whiff=0); token-gate slow-mo/expose restores; respect-counter consumes only on a landed hit.
- [VPS SSH connection gotchas](vps-ssh-credentials.md) — a private-key secret may be stored single-line (re-wrap before ssh-keygen); unreachable VPS on all ports = host-side, not sandbox egress; wss needs a domain+TLS, not a bare IP.
- [Animator Voxel LED Mask](animator-led-mask.md) — face is a real voxel LED dot-matrix (InstancedMesh), NOT a 2D canvas; expressions glyphless + banner on its own strip (never on the visor); faces dissolve/re-form via per-cell radial+noise reveal; keep all Color work on scratch instances.
- [Animator weapon hold-style standard](animator-hold-style-standard.md) — ONE per-WeaponGroup standard owns grip/reach/combat-default/fight-band/guard-pose/defense-clips; weapons declare only deviations (combat is Partial); ranged spaces by fightBand kite distance not its tiny melee range.
- [Animator throwable consumables](animator-throwable-consumables.md) — throwables reuse `throw` clip (no-ops on GLB) + thrownProp/aoeBlast/sparringBlast; heal MUST go through CombatController.heal (sparring owns HP, raw this.health write is overwritten / fights PvP authority).
- [Animator arcane-staff & blink](animator-arcane-staff.md) — bespoke caster kits branch useSkill on def.flag+weaponId before the shared skillCooldown gate; true teleport must use Controller.blinkTo (resets fall/dash/velocity), not a raw root.position set.
- [Animator combat audio](animator-combat-audio.md) — pooled spatial WebAudio one-shots + ambient bed; resume() on gesture, mute via post-mix master gain (LFO bleeds bed gain), StereoPanner feature-gated.
- [Animator root-lock baseline](animator-root-lock-baseline.md) — lockHorizontalRoot must center hip X/Z to BIND pose (not clip frame-0) + match the hips track tolerantly; retargeted block/extra/reactions packs author roots ~60u off-origin → "feet meters away".
- [Animator FBX skeleton normalization](animator-fbx-skeleton-normalization.md) — box rig binds clips by EXACT mixamorig* names; non-mixamorig "Retargeted Clip" FBX packs (whole great-sword family) silently bind to nothing → dead 2H loco; loadClips auto-normalizes them (rotation + root-pos only).
- [Animator Tank/Centurion kit](animator-tank-kit.md) — slow+armoured gunblade+scutum char reuses "sword" animSet; tankiness = damage-mult in resolveOpponentStrike (not max-HP); any speed reset MUST restore to baseSpeedMul() not bare 1, or the slow identity is lost after recovery.
- [Animator device/input UX system](animator-device-input-system.md) — touch UI gated on capability (matchMedia pointer/hover + maxTouchPoints + iPadOS-as-Mac), NOT viewport width; classifyDevice is pure+tested; canvas resize needs ResizeObserver+DPR-reapply+zero-guard; mobile needs viewport-fit=cover + dvh + safe-area padding on touch-layer.
- [Racalvin pirate = Meshy single-GLB](animator-racalvin-meshy.md) — "Racalvin the Pirate King" = id `gunslinger` (now a GLB, kiter kit dropped); Meshy packs ship 1 skin-bundled GLB/clip (~9MB ea) sharing one skeleton → merge tracks-only via scripts/src/merge-glb-anims.mjs (no retarget).
- [GLB library retarget](animator-glb-library-retarget.md) — real GLB chars play shared mixamorig FBX library via SkeletonUtils.retargetClip; source must be a SkinnedMesh, rename `.bones[X].quaternion`→`X.quaternion` rotation-only; no `preserveBonePositions` key in r0.184; cancel-after-await must unregister+remove root not just dispose.
- [Animator Skill Lab](animator-skill-lab.md) — Playground live authoring; target=playAvatar??selected, re-apply on EVERY target swap or state desyncs; bound the authored sub-clip cache; source GLB VFX are in vfxCatalog already (don't re-import Godot fx).
- [Animator HUD editor](animator-hud-editor.md) — additive 2D HUD-layer editor; theme styling MUST be class-gated under `.studio.hud-themed` (default=no class=stock look), not value-matched; reticle/status always get editBind so gate visibility on editing (className includes "hud-editable"), not editBind existence.
- [Animator Hot Hands fire-combo](animator-fire-combo-hothands.md) — caster F-key (non-sig useSkill) chained ability hooks BEFORE the skillCooldown gate w/ own combo lock; reset combo state in recoverFromFail + update loop; pickTargetInFront already = "@target"; hotHands VFX is owned-instances (safe dispose).
- [Animator ability lifecycle orchestrator](animator-ability-lifecycle.md) — pure data-driven AbilityDef + cast→release→travel→impact→status state machine in src/three/abilities; update(dt) MUST sit beside updatePending(dt) for same-frame impacts; cancelAll wherever pending clears.
- [FBX/GLB→GLB conversion in Node](animator-fbx-node-conversion.md) — convert-character CLI: gltf-transform reads GLB (GLTFLoader hangs in Node); FBX needs three+DOM/Blob/FileReader shims & textures dropped; normalize onto root TRS (idempotent); strip non-Hips pos only if mixamorigHips exists; spec-gloss GLBs need ALL_EXTENSIONS + metalRough() or they render black.
- [Tailwind v4 content-scan OOM](tailwind-v4-content-scan-oom.md) — bare `@import "tailwindcss"` auto-scans from git root (gitignore-aware); a huge non-ignored workspace dir (attached_assets 3.9GB) OOM-kills vite on first CSS request (native, dodges --max-old-space); fix = `source(none)` + scoped `@source`.
- [Agent bash process gotchas](agent-bash-process-gotchas.md) — detached servers die when the bash call returns (only the workflow harness keeps a dev server alive); pkill -f matches the agent's OWN command line (exit 143 = self-kill, use vite[.]config regex); screenshot needs workflow "running" not just port 200.
- [Animator in-play loadout overlay](animator-equipment-overlay.md) — live weapon swap during play/danger; consumes studio.setWeapon (additive); Esc must be handled BEFORE the INPUT keyguard (autofocus search); reset on mode change.
- [Animator Danger Room dummy models](animator-dummy-models.md) — passive training dummies are converted GLBs (2m, feet@0) cloned via SkeletonUtils sharing geo/mats (dispose-once); play own clips (no retarget); frozen-frame-0 for punch/damaged rigs keeps them passive; hit volume is pure chest() math so hiding meshes is safe.
- [Animator Exo-Armour Mech Mode](animator-exo-armour-mech.md) — KeyM (not KeyG=evade) suit-up into a rideable mech; pure mechState SM + cached-GLB clone; any avatar-takeover (duel spectating, phantom expiry) must reconcile mech visibility/speed via cancelMech / `!mech.isActive` guards.
- [Animator elemental magic staffs](animator-elemental-staffs.md) — fire/ice/storm/nature/holy each own a WeaponId+6 cane skins; cast is data-driven via WeaponDef.element + ELEMENT_THEME, NOT per-character kits; useSkill branches on weapon element before the shared gate.
- [Animator GLB fluid blending](animator-glb-fluid-blending.md) — ported explorer weight-blend loco + upper-body additive overlay + fraction-snippets onto GLB Character; overlay must NOT set one-shot flag (Controller freezes legs); subclip is end-exclusive (~1 frame slop); one shield per block fired at SFX sites, removed from resolveOpponentStrike.
- [Animator Danger Room presets](animator-room-presets.md) — data-driven holo/foundry/colosseum envs; door+DJ+combat coords stay preset-independent; grid visible = gridWanted && preset.gridOpacity>0; sessionStorage dangerroom:preset.
- [Animator room poster strip + home routing](animator-ledmask-room-gallery.md) — RoomGallery poster strip is the PHONE home (doors mode, touchUI-gated), removed from LED Mask; desktop home = DoorSelect behind the Grudge ID landing.
- [Animator app shell + global AI](animator-app-shell.md) — one AppShell overlay wraps every mode (launcher pill/nav + ONE AiAssistant); per-surface config from App base prop OR child useRegisterAssistant; ledmask hides it (own face chat); voice via useVoiceChat.
- [Voxel Editor object editor](animator-voxel-object-editor.md) — Select+TransformControls gizmo edits deployables (not blocks); `data.scale` is a PURE user multiplier, editor composites difficulty base (deployBaseScale) for preview, writeback divides it back out so it round-trips to play mode.
- [Animator foot-to-ground IK wiring](animator-foot-ik-grounding.md) — post-mixer FootGrounder OFF by default (flat room untouched), enabled only in dungeon; navmesh stays THREE-free so the Vector3 sampler adapter lives in Studio; non-finite y = off-ground no-op; runs LAST as apply(dt).
- [Animator targeting/lock controls](animator-targeting-controls.md) — three split systems: always-on cone-gated soft-lock (Tab cycle / Alt+Tab off), RMB toggle hard lock, Ctrl-hold block (+Ctrl+Space air block); Ctrl is hazardous (preventDefault) & block auto-releases on pointer-unlock.
- [Animator persistent lobbies](animator-persistent-lobbies.md) — official rooms (Danger Room/Colosseum) are a per-room `persistent` flag: exclude from BOTH reap paths, list while empty, reset-on-empty; spec lives in danger-net for testability; seeded codes can't collide.
- [Voxel template/editor grounding](animator-voxel-template-grounding.md) — editor grounds deployables on the block BELOW their cell; author them at floorCell+1 or they bury; play mode hides it (physics). rigs face +Z → yaw=atan2(dx,dz).
- [Animator combo attack-clip choice](animator-combo-attack-clip.md) — doComboHit replays the SAME attack-role clip every stage; a multi-swing attack clip makes ONE LMB look like a slow auto-combo. Keep clips.attack a single snappy swing.
- [Animator landable obstacle tops](animator-landable-obstacle-tops.md) — ObstacleCircle.top = walkable surface; support sampling gates on fall-from height; push-out/dash skip at top; specials anchor to supportY; rigs land via notifyLanded not root.y.
- [Animator Avatar Edit head builder](animator-avatar-edit.md) — pure pixel-face composer + protrusion boxes (testable sans WebGL); WebGLRenderer-in-effect must catch + degrade or the React tree dies; door posters must match the neon voxel card style.
- [Animator avatar GLB hats](animator-avatar-hats.md) — Sketchfab roots may carry sloppy display-pose quats (snap to exact -90°X); pack nodes vary facing axis; verify offline via point-cloud rasterize.
- [Cloudflare Pages+R2 deploy](animator-cloudflare-deploy.md) — Cloudflare builds set VITE_ASSET_BASE_URL to R2 and strip media from Pages upload; sandbox wrangler/token gotchas inside.
- [Animator player predictive lead](animator-player-predictive-lead.md) — player aimed projectiles lead moving targets via TargetHandle.velocity (smoothed finite-diff of dummy pos, NOT physics vel); any new TargetHandle literal must supply velocity (handles in Targets.ts + DungeonEnemies.ts).
- [Avatar Edit player head bridge](animator-avatar-player-head.md) — saved head dresses ONLY the Explorer rig (flag can't live in shared DEFAULT_LOOK); material swap decouples head from skin recolor; protrusions head-unit scaled.
- [Animator persistent music](animator-persistent-music.md) — app-level dual-deck musicStation singleton; CombatSfx.dispose() must NOT stop it; radio stations (local + Audius) re-assert via assertStation, async switches token-guarded.
- [Animator animation debugger](animator-anim-debugger.md) — opt-in animDebug singleton + dock panel; validate ORIGINAL clip pre-filter (filtered is always 100% bound); observational hooks only; log getWorldPosition; push must return a new array ref for useSyncExternalStore.
- [Animator weapon tuning persistence](animator-weapon-tuning-persistence.md) — grip/size/blade-collider edits persist to localStorage + apply onto the SHARED catalog at Studio+EditorScene ctor (so Dressing Room edits reach combat); tier variants need tier-aware keys or edits silently lost.
- [Voxel-world dungeon stages](animator-voxel-world-stages.md) — big voxel-world GLBs ship as Dungeon stages via meshopt (~10x); MUST wire MeshoptDecoder into the Dungeon loader or they silently fail; ~1GB sources OOM the optimizer.
- [Playground Play HUD + weapon skills](animator-playground-play-hud.md) — Dressing Room Play game HUD: data-only weapon-group skill kits, engine stamps cooldown timestamps + emits on fire, UI self-animates sweep; gate the rAF on readyAt>now (some(cooldown>0) is always true → 60fps churn).
- [Heroes of Grudge prefabs](animator-heroes-of-grudge.md) — 24 Bip001 self-animating GLB heroes (6 races × 4 class kits); CharacterDef.loadout/offHand/hideNodes; Q cycles loadout (else parry); baked weapons hidden by regex so library weapon shows.
- [Danger Room backdrops](animator-backdrops.md) — full-scene battle-art via scene.background=Texture; safe ONLY because every fog/water/preset bg write guards on `instanceof THREE.Color`, so a texture bg is left untouched.
- [Animator mesh-fit weapon collider](animator-weapon-mesh-collider.md) — blade capsule + Dressing Room wrap fitted from real mesh slices (edge-sampled); def.hit wins; tune central pad not per-weapon.
- [Animator unit-frame status HUD](animator-unit-frames.md) — gold player/target frames skip themed rpg-* classes; target frame force-renders in HUD-edit; verify HUD DOM headless via a throwaway vite root .html harness (no WebGL).
- [Player badge / portrait / GBUX](animator-player-badge-gbux.md) — GBUX soft currency store (route earn/spend via addGbux); WebGL-free 2D head portraits for thumbnails; doors screen scrolls via auto-margin centering.
- [Animator Toolbox launcher](animator-toolbox.md) — shell 5×5 tool grid; child-mode panel launches need a buffered request bus; App-state resets need a one-shot pending ref consumed inside the reset effect.
- [Identity-rest FBX bake](animator-identity-rest-fbx-bake.md) — all-zero-rest FBX packs (gestures) break runtime normalize AND naive retargetClip; bake offline with rest-pose localOffsets → native GLB clip ids.
- [Animator global tooltip layer](animator-tooltips.md) — use `data-tip` (one shell-mounted TipLayer), never native `title=`; keep aria-label on icon-only controls; touch = long-press, keyboard = :focus-visible.
- [Phantom wallet linking](animator-phantom-wallet.md) — @phantom/browser-sdk needs a Vite buffer alias polyfill (silent runtime break otherwise); "embedded" invalid provider; nonce ownership-proof must be byte-identical both sides; pure walletVerify helpers are unit-tested.
